export function direct(component,input) {
  component.innerText = input;
}
//function just take component i.e. given and just fill its inner-text to given input 
//this seems tempting to use but if working is massive it cud even crash the server